name: "dw_hr"
version: "1.0.0"
config-version: 2

profile: "dw_hr"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dw_hr:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
